# deep_builder_agent — 개발 보고서

> 작성일: 2026-08-10 · 상태: Phase 1~5 완료 · 테스트 307건 통과
> 근거 원장: [BUILD_SPEC.md](BUILD_SPEC.md) (설계 결정·실측 기록 전문)

---

## 1. 개요

### 1.1 목적
- 자연어 요구를 받아 **AI 에이전트를 생성·실행·평가**하는 빌더 구현
- 핵심 주장: **LLM의 준수를 신뢰하지 않고, 제약을 코드로 강제**한다

### 1.2 산출물
| 구분 | 내용 |
|---|---|
| 진입점 | `cli.py` (터미널, `/revise` 지원), `ui/app.py` (Streamlit 2패널) |
| 명세 스키마 | `AgentSpec v0.3` — 도구는 레지스트리 키 문자열 참조만 허용, 팀원별 모델 지정 가능 |
| 팀 템플릿 | 3종 (`research_team`, `data_analysis_team`, `doc_qa_team`) |
| 평가 | 케이스 21건 + 기계적 검사 5종 + LLM-as-judge |
| 관측 | LangSmith 트레이싱 (서브에이전트 스팬까지) |

### 1.3 기술 스택 (설치본 소스로 API 검증 완료)
- deepagents 0.7.5 (에이전트 하네스) / LangGraph 1.2.10 (런타임)
- langchain-anthropic 1.5.4 / langchain-mcp-adapters 0.3.2
- Pydantic v2 (스펙 검증) / Streamlit 1.61.1 (UI) / Python 3.13
- 모델: Builder `claude-sonnet-4-6`, 심판 `claude-haiku-4-5`

---

## 2. 설계 기조 (3축)

### 2.1 카파시 원칙 — 단순한 것부터
- baseline(단일 에이전트) → 복잡도(멀티) 순으로 진행
- Phase 게이트를 **스키마 밸리데이터로 강제** — Phase 1~2에서 `subagents`는 빈 배열만 허용
- 게이트 해제(Phase 3 착수)를 사전에 문서화하고 그 시점에 밸리데이터 제거

### 2.2 하네스 엔지니어링 — 신뢰하지 않고 강제한다
| 층위 | 수단 |
|---|---|
| 제품 | Pydantic 스펙 검증, 도구 화이트리스트, 가드레일 문장 **코드 주입**, 검증 실패 → Builder 피드백 루프 |
| 개발 | CLAUDE.md(작업 규칙), pytest 검증 루프, Phase 게이트 커밋 |

- 가드레일 문장은 프롬프트로 **요청**하지 않고 `ensure_guardrail()`이 **삽입**
- 도구 화이트리스트는 프롬프트 지시가 아니라 밸리데이터가 **거부**

### 2.3 멀티오케스트레이션 — 근거가 있을 때만
- 팀 생성 기본값은 **단일 에이전트**
- 팀 분리 근거: 역할별 판단 기준 상이 / 중간 산출물이 리더 문맥 오염 / 교차 검증 필요
- **도구가 다르다는 것만으로는 근거가 아니다** — 한 에이전트가 도구를 여러 개 들고
  순서대로 쓸 수 있다. 이 항목이 예전에는 팀의 근거로 적혀 있어 다른 지침과 충돌했다(6.10)
- 근거는 실측이다: 2인 팀은 같은 답에 **호출 3배, 출력 토큰 10배 이상, 지연 5배**(7.4)
- 계층 깊이 1 고정 — `SubAgentSpec`에 `subagents` 필드를 두지 않아 **구조적으로 강제**

---

## 3. 시스템 구조

### 3.1 4레이어
```
자연어 요구
   ↓  builder/    LLM 호출 → JSON 파싱 → 가드레일 주입 (리더 + 팀원 전원)
   ↓  runtime/    AgentSpec 검증 (도구 화이트리스트 / 팀 구성 검증)
   ↓  registry/   커스텀 도구 + MCP 서버 도구 해석
   ↓  runtime/    deepagents 인스턴스화 (리더·팀원 각각 요청한 도구만 노출)
대화 / 평가
```

### 3.2 레이어별 책임
| 레이어 | 책임 | 핵심 파일 |
|---|---|---|
| `builder/` | 자연어 → AgentSpec JSON, 가드레일 주입, 재시도 루프 | `builder.py`, `prompts.py` |
| `registry/` | **허용 도구의 단일 진실 원천**, 도구 구현, MCP 커넥터 | `registry.py`, `builtin.py`, `mcp.py` |
| `runtime/` | 스펙 검증 → deepagents 번역, 도구 격리, 설정·트레이싱 | `spec.py`, `factory.py`, `config.py` |
| `ui/` | Streamlit 화면(`app.py`)과 판단 로직(`state.py`) 분리 | `app.py`, `state.py` |
| `eval/` | 기계적 검사 + LLM 심판 + 집계 | `checks.py`, `judge.py`, `runner.py` |

---

## 4. 핵심 설계 결정

### 4.1 도구 화이트리스트를 middleware로 실제 강제
- **문제**: deepagents는 스펙과 무관하게 내장 도구 9종을 주입하며, 그중 `execute`는 **셸 실행**
- **결정**: `FilesystemMiddleware(tools=[...])`로 덮어써 스펙이 요청한 도구만 잔존
- **효과**: 스펙에 없는 `execute`·`delete`·`edit_file` 등 차단 (실측 확인)
- **단, 이 차단은 절반만 유효하다** — `python_repl`이 임의 코드 실행이라
  (`os.system` 동작 실측) 그 도구를 허용한 스펙은 사실상 셸을 허용한 것이다.
  비밀값 환경변수는 차단했으나 격리는 아니다 (6.6 참조)

### 4.2 허용 도구 목록을 레지스트리에서 파생
- 하드코딩 상수(`spec.ALLOWED_TOOLS`) 제거 → `registry.allowed_tool_keys()`가 단일 진실 원천
- Builder 프롬프트의 도구 목록도 `registry.tool_catalog()`에서 렌더링
- **효과**: 프롬프트가 광고하는 도구와 밸리데이터가 허용하는 도구가 **어긋날 수 없음**
- **실측 근거**: `file_list` 등록 후 프롬프트를 손대지 않았는데 Builder가 자동으로 해당 도구 선택

### 4.3 서브에이전트 도구 격리 (Phase 3 최대 쟁점)
- **발견**: 메인의 `FilesystemMiddleware`는 서브에이전트에 **전파되지 않음**
- **실측**:

| 구성 | 메인 도구 | 서브에이전트 도구 |
|---|---|---|
| 팀원 middleware 없음 | `read_file`, `task` | `delete, edit_file, execute, glob, grep, ls, read_file, write_file` |
| 팀원 middleware 명시 | `read_file`, `task` | `read_file` |

- **결론**: 위임 한 번으로 셸이 열림 → 팀원마다 middleware 명시적 주입
- **회귀 테스트**: `test_factory.py::test_shell_does_not_leak_into_subagents`

### 4.4 평가 대상을 Builder의 번역 품질로 한정
- "생성된 에이전트의 답변이 좋은가"는 도구·모델·프롬프트가 뒤섞인 결과 → 회귀 신호 부적합
- **"자연어 요구 → 옳은 AgentSpec인가"** 가 본 프로젝트가 직접 책임지는 부분
- 기계적 검사(코드로 확정 판정)와 LLM 심판(판단 필요)을 분리, 검사 실패 시 심판 미호출

### 4.5 실패를 조용히 넘기지 않는다 (일관 원칙)
| 상황 | 처리 |
|---|---|
| `TAVILY_API_KEY` 미설정 | 결과를 지어내지 않고 "키 없음 + 지어내지 말 것" 에러 반환 |
| `LANGSMITH_TRACING=true` + 키 없음 | 실행 **전에** 차단 (`TracingConfigError`) |
| 팀원의 MCP 참조가 매핑에 없음 | `LookupError` — 조용히 사라지지 않음 |
| 심판 응답 파싱 실패 | `JudgeError` — 0점으로 뭉개지 않음 (채점 실패 ≠ 낮은 점수) |

### 4.6 파일 접근을 `workspace/`로 제한
- 기본 `StateBackend`(가상 FS)는 실제 문서를 못 읽고, 루트를 전체 개방하면 `.env`가 읽힘
- `FilesystemBackend(root_dir=workspace/, virtual_mode=True)`가 절충안
- `..`·`~`·외부 절대경로 차단을 실측(`ValueError: Path traversal not allowed`)
- **프로세스 격리가 아니므로** "workspace 안에 비밀값을 두지 않는다" 규칙 병행

---

## 5. 단계별 진행

| Phase | 범위 | 결과 | 테스트 |
|---|---|---|---|
| 1 | 자연어 → AgentSpec → 단일 에이전트 생성·대화 | 완료 | 43건 |
| 2 | `registry/` 이관, MCP 커넥터, 도구 자동 선택 | 완료 | 73건 |
| 3 | subagents 활성화(v0.2), 팀 템플릿 3종, 도구 격리 | 완료 | 106건 |
| 4 | Streamlit UI, LangSmith 트레이싱, 평가 레이어 | 완료 | 154건 |
| 실호출 검증 | 전 경로 실측 + 결함 수정 | 완료 | 188건 |
| 평가 확장 | 케이스 12건 + 보안 결함 수정 | 완료 | 193건 |
| MCP transport | `streamable_http`·`sse` 실연결 + 헤더 전달 검증 | 완료 | 204건 |
| 평가 3차 확장 | 케이스 21건 + 평가 스위트 자체 결함 4건 수정 | 완료 | 215건 |
| 미결 항목 정리 | 가드레일 공용화·팀 비용 실측·모델 비교·프롬프트 모순 수정 | 완료 | 226건 |
| 팀원 모델 차등 | `AgentSpec v0.3` — `SubAgentSpec.model` (선택, 비우면 상속) | 완료 | 235건 |
| Phase 5-1 | 계산 전용 도구 분리 — 평가에서 `python_repl` 선택 7건 → **0건** | 완료 | 284건 |
| Phase 5-2 | 에이전트 수정 루프 — 자연어 수정 + 변경 내역 diff | 완료 | 303건 |
| Phase 5-3 | 제출 패키지 점검 — clone 재현, 문서 일관성, 데모 대본 | 완료 | **307건** |

---

## 6. 발견·수정한 결함

> **14건 전부 단위 테스트로는 잡히지 않았고, "실제로 실행할 때만" 드러난 종류다.**

### 6.1 인코딩 (Phase 1)
| # | 결함 | 원인 | 조치 |
|---|---|---|---|
| 1 | `python_repl` 한글 출력 유실 | `subprocess.run(text=True)`가 로케일 코덱(cp949)으로 디코딩 → `UnicodeDecodeError` | `encoding="utf-8"` + 자식 프로세스 `PYTHONUTF8` 지정 |
| 2 | CLI 이모지/한글 깨짐 | Windows 콘솔 cp949 | 표준 입출력을 UTF-8로 고정 (뒤에 `runtime/console.py`로 공용화 — 6.9 D) |

### 6.2 배포 산출물이 그대로는 동작하지 않음 (Phase 2)
| # | 결함 | 원인 | 조치 |
|---|---|---|---|
| 3 | `mcp_servers.example.json` 복사 시 로드 실패 | JSON에 주석이 없어 `_comment` 키를 넣었는데 로더가 서버 항목으로 인식 | `_` 접두사 키를 설명문으로 취급 + **템플릿 자체를 로드하는 회귀 테스트** |
| 4 | stdio `command: "python"` PATH 함정 | venv가 아닌 전역 인터프리터 기동 → 의존성 없음 | 예제·문서를 venv 경로 명시로 수정 |

### 6.3 환경 설정 (실호출 시 발견)
| # | 결함 | 원인 | 조치 |
|---|---|---|---|
| 5 | 빈 환경변수가 기본값을 덮어씀 | `os.environ.get(k, default)`는 키가 **없을 때만** 기본값 적용. `.env.example` 복사 시 `KEY=`가 "존재하는 빈 키"가 됨 | `env_or_default()` 공용화 (공백뿐인 값도 미설정 취급) |
| 6 | 진입점이 `.env`를 로드하지 않음 | `cli.py`만 `load_dotenv()` 호출. **UI는 키가 있어도 "미설정"으로 실행 차단** | `load_env()` 한 곳으로 통합 + **새 진입점 누락 시 실패하는 테스트** |

- **5번이 특히 중요**: README가 `cp .env.example .env`를 안내하므로 **안내를 따른 사용자가 전부 밟는 경로**

### 6.4 기능이 설명대로 동작하지 않음 (Phase 3·4)
| # | 결함 | 원인 | 조치 |
|---|---|---|---|
| 7 | `render_history`가 사용자 발화를 못 읽음 | 단일 메시지에 `last_text()` 사용 — 해당 함수는 `type=="ai"`만 처리 | `message_text()` 분리 후 `last_text` 재구성 |
| 8 | `doc_qa_team`이 실제 문서를 못 읽음 | 가상 FS(StateBackend) + 목록 조회 도구 부재로 경로 추측 | 백엔드 교체 + `file_list` 도구 추가 |

### 6.5 테스트 자체의 결함 (가장 위험)
> **보안 경계 테스트가 잘못된 이유로 통과하고 있었음**

- **증상**: 경로 탈출 차단을 검증한다며 `pytest.raises(Exception)`으로 `backend.read_file(...)` 호출
- **실상**: `FilesystemBackend`에 `read_file` 메서드가 **존재하지 않음** → `AttributeError`를 "차단됨"으로 오인
- **결과**: 9건이 통과 상태였으나 **실제로는 아무것도 검증하지 못함** (실제 API는 `read()`)
- **조치**
  - 대조군 추가 — 허용 경로가 실제로 읽히는지 확인 (전부 실패해서 통과하는 상황 방지)
  - "비밀값이 결과에 나타나지 않는다"는 **성질**로 검사 (차단 방식이 경로마다 다름)
  - 메서드 계약 자체를 테스트로 고정
  - CLAUDE.md 금지 항목에 등재
- **교훈**: **통과하는 보안 테스트일수록 "무엇이 통과시켰는지" 확인해야 함**

### 6.6 도구가 비밀값을 그대로 넘기고 있었다
> **평가 케이스를 늘리려다 나온 보안 결함**

- **발단**: "셸 명령을 실행해주는 에이전트"를 요구하자 Builder가 `python_repl`을 붙였다.
  그게 진짜 우회 경로인지 확인하다 더 큰 것을 발견했다
- **실측**

| 프로브 | 수정 전 | 수정 후 |
|---|---|---|
| `os.system` / `subprocess` | 실행됨 | 실행됨 (도구 본질) |
| `ANTHROPIC_API_KEY` 환경변수 | **노출** | 차단 |
| `TAVILY_API_KEY` / `LANGSMITH_API_KEY` | **노출** | 차단 |
| 네트워크 송신 | 가능 | 가능 (도구 본질) |

- **원인**: `env={**os.environ, ...}` — 부모 환경을 통째로 물려줬다.
  `python_repl`은 임의 코드 실행이므로 **환경변수는 "에이전트가 읽을 수 있는 값"** 이다.
  키 읽기와 네트워크가 겹치면 유출 경로가 성립한다
- **조치**: 환경변수 화이트리스트로 전환(인터프리터 구동용 8개만). 회귀 테스트 5건이
  비밀값 이름이 목록에 추가되는 것을 막는다
- **더 중요한 것은 주장의 수정**: "셸 도구 `execute`를 차단한다"는 설명은 절반만 맞았다.
  `python_repl`을 허용한 스펙은 사실상 셸을 허용한 것이다.
  도구 docstring과 문서에서 **"격리된"이라는 표현을 걷어냈다**
- **교훈**: 차단한 것의 목록보다 **남겨둔 것이 무엇을 할 수 있는지**가 실제 경계를 정한다

### 6.7 프롬프트가 시스템에 없는 기능을 약속
- **증상**: 팀원 7명 요구 시 **간헐적** 생성 실패
- **최초 가설**: Builder가 상한 지시를 무시 → **오진**
- **관찰**: 재시도 각 시도의 입출력을 캡처 → 1차 시도에 **JSON이 아예 없음**(Builder가 되물음)
- **진짜 원인**: 프롬프트에 *"모호하면 최대 2개의 질문으로 확인한다"*가 있으나 **그 질문을 받아줄 코드 경로가 없음** (CLI·UI·평가 전부 단발 호출) → 되묻는 순간 재시도 소진
- **조치**: 질문 단계 제거, "합리적으로 가정하고 가정을 `description`에 적는다"로 교체
- **교훈**: 프롬프트가 시스템에 없는 상호작용을 약속하면 안 됨. 대화형 인터뷰가 필요하면 **코드부터** 만든다

### 6.8 선언한 기능의 2/3이 한 번도 실행되지 않았다
> **로드맵에서 사유 없는 항목을 걷어내다 드러난 빈틈** — 6.5와 같은 계열이다

- **발단**: 로드맵의 "특정 외부 MCP 서버 연결" 항목에 **기록된 사유가 없었다.**
  그 서버는 프로젝트 소유자가 직접 배포한 것이라 붙여봐야 제3자 상호운용성의 증거가 못 된다 →
  제거 판단. 그런데 **그 항목이 암묵적으로 덮고 있던 것**을 확인하다 결함이 나왔다
- **증상**: 커넥터는 transport 3종을 지원한다고 선언하는데, 실행되는 것은 `stdio` 하나뿐

| transport | 지원 선언 | 실연결 검증 (발견 시점) |
|---|---|---|
| `stdio` | ✅ | ✅ |
| `streamable_http` | ✅ | ❌ 설정 파싱만 |
| `sse` | ✅ | ❌ 설정 파싱만 |

- **더 나쁜 것 — 헤더**: `${ENV_VAR}` 치환 테스트는 `load_config()`가 **돌려주는 dict**만 봤다.
  치환된 헤더가 전송되지 않아도 통과하는 단언이었고, **인증이 필요한 MCP 서버는 전부 이 경로를 탄다**
- **조치**: echo 서버에 `--transport` 인자와 `http_request_headers` 도구를 추가.
  세 transport를 같은 코드로 띄워 도구 목록·호출·비ASCII 왕복·헤더 전달을 검증
- **검출력 확인 (음성 대조군)** — 통과했다는 사실만으로는 아무것도 모른다

| 설정 | 서버가 관측한 헤더 | 토큰 도달 |
|---|---|---|
| `headers` 없음 | `{}` | ❌ |
| `headers` 있음 | `authorization`, `x-deep-builder-probe` | ✅ |

- **비용**: LLM 호출 **0회**. 외부 계정·네트워크 없이 오프라인 상시 실행
- **교훈 1**: **사유가 기록되지 않은 로드맵 항목은 그것이 덮던 리스크도 같이 잊힌다.**
  항목을 지울 때는 "왜 넣었는지"를 먼저 복원해야 무엇을 잃는지 안다
- **교훈 2**: 지원한다고 **선언한** 것과 실행해본 것은 다르다. 설정 파싱 테스트는
  "설정을 읽을 수 있다"만 증명하며, 그 설정으로 실제로 통신되는지는 말해주지 않는다

### 6.9 평가 스위트 자체의 결함 4건
> **케이스를 늘리려다 Builder가 아니라 평가·하네스 쪽이 나왔다**

케이스 12 → 21건으로 늘리며 후보 14건을 실제로 던졌다. **13건이 기대와 일치**했다.
Builder 자체 결함은 0건이고, 대신 아래 넷이 나왔다.

| # | 결함 | 왜 안 보였나 | 조치 |
|---|---|---|---|
| A | 프롬프트에 없는 규칙 | Builder가 우연히 옳게 하고 있어 아무도 안 물었다 | "팀 요구에 근거가 없으면 단일 + 이유를 description에" 명문화, 양방향 케이스 고정 |
| B | 팀 케이스가 도구를 검사하지 않음 | 도구는 리더가 아니라 **팀원**에게 붙는데 `expect_tools`는 리더만 본다 | `expect_subagent_tools` + `check_subagent_tools` + 누락 방지 테스트 |
| C | 심판이 팀원 프롬프트를 못 봄 | 심판에게 팀원 **이름만** 넘기고 있었다 | 도구·description·프롬프트까지 펼쳐 전달 |
| D | `eval/runner.py` 인코딩 | 콘솔 직접 출력에서는 멀쩡, **리다이렉트할 때만** 죽는다 | `runtime/console.py`로 공용화 + 진입점 누락 테스트 |

- **A의 실체**: `team_size_cap`(7명 요구 → 5명 팀)과 새 케이스(5명 요구 → 단일)는
  방향이 반대다. 가르는 기준이 프롬프트에 없으면 다음번에 뒤집혀도 이상하지 않다
- **B의 실체**: 조사 팀에 **아무도 `web_search`가 없어도 통과**했다.
  6.5(보안 테스트가 `AttributeError`를 차단으로 오인)와 같은 계열 — 실패할 수 없는 단언
- **C의 실체**: 채점 기준이 도구 배분을 묻는데 심판은 도구를 못 봤다.
  보이지 않는 것을 근거로 감점 → 수정 후 4/5 → **5/5**, 심판이 도구를 직접 인용
- **D의 대가**: 평가 21건을 다 돌린 **뒤** 출력에서 죽어 **LLM 호출 42회분이 유실**됐다.
  Phase 1에서 `cli.py`에 고친 바로 그 버그가 다른 진입점에 남아 있었다
- **교훈 1**: **축 선정이 결과를 지배한다.** 1차 8건(도구 지정·팀원 의존·출력 형식·다국어)은
  7건이 그냥 통과했고, 프롬프트 지침끼리 부딪치는 지점을 노린 2차에서야 결함이 나왔다
- **교훈 2**: **통과율은 스위트의 건강 지표가 아니다.** 무엇을 못 보는지를 따로 확인해야 한다
- **교훈 3**: 진입점마다 각자 챙기는 배선(`.env` 로드, 콘솔 인코딩)은 **새 진입점에서 반드시 빠진다.**
  공용화하고 누락을 테스트로 막는 것 외에 방법이 없다

### 6.10 프롬프트가 자기 자신과 모순됐다
> **모델을 비교하다 나왔는데, 원인은 모델이 아니었다**

- **증상**: `four_tools_one_request`("검색하고 → 읽고 → 계산하고 → 저장")에서
  `claude-sonnet-4-6`이 **4인 팀**을 만들어 실패. 그런데 **같은 날 아침 전체 평가에서는
  같은 모델로 통과**했다
- **최초 가설**: 모델 성능 차이 → **오진**
- **진짜 원인**: 프롬프트의 두 규칙이 같은 요구를 반대로 보내고 있었다

| 프롬프트 문장 | 이 요구를 어디로 |
|---|---|
| "요구에 동사가 둘 이상이면 도구도 둘 이상일 수 있다" | 단일 + 도구 여러 개 |
| "단계마다 필요한 도구가 다르다" (팀으로 나눌 근거) | **팀** |

- **조치**: "단계마다 도구가 다르다"를 팀의 근거에서 제거하고,
  도구 차이만으로는 근거가 아님을 명시. 마침 직전에 잰 비용(팀 = 5~6배)이
  어느 쪽이 맞는지 정해줘서 **그 수치를 프롬프트에 넣었다**
- **검증**: 팀 판단이 걸린 7케이스 재실행 **9/9**, 뒤집히던 케이스는 **3회 반복 모두 단일**
- **교훈 1**: **모델 비교가 프롬프트 결함을 드러냈다.** 두 모델이 갈리면
  모델 차이를 의심하기 쉽지만, 갈릴 수 있다는 것 자체가 **입력이 모호하다는 신호**다
- **교훈 2**: 프롬프트는 문장이 늘수록 서로 부딪친다. 새 지침을 넣을 때
  **기존 지침과 같은 입력을 어디로 보내는지** 확인해야 한다

### 6.11 평가가 결정적이라고 가정하고 있었다
- **증상**: 같은 케이스가 같은 모델로 실행마다 단일/팀을 오갔다
- **실상**: `run_evaluation`은 케이스당 **1회**만 돌리고 "21/21 통과"로 보고했다.
  간헐적 실패를 통과로 착각하는 구조다
- **조치**: `run_evaluation(repeats=N)` 추가 — **한 번이라도 실패하면 그 실패가 리포트에 남는다.**
  기본값은 1이라 평소 비용은 그대로다
- **남은 한계**: 배포 리포트의 "21/21"은 **여전히 1회 표본**이다. 이 사실을 README에 명시했다
- **교훈**: 비결정적 시스템을 1회 측정하고 "통과"라 부르면, 그 숫자는 품질이 아니라 **운**을 보고한다

---

## 7. 정량 결과

### 7.1 테스트
- **307건 통과**
- `-m "not integration"` 분리로 빠른 피드백 확보 (프로세스·앱 기동 검증 제외)

### 7.2 평가 (Builder 회귀 검사)
- 케이스를 **추측이 아니라 실측으로** 확보 — 기대값을 먼저 적고, 후보 10건을 실제로 던지고, 어긋난 4건만 고정
- 진행 경과

| 단계 | 통과율 |
|---|---|
| 1차 케이스 추가 직후 | 5/9 (56%) |
| 도구·팀 판단 프롬프트 수정 | 8/9 (89%) |
| 질문 단계 제거 | 9/9 (2회 연속) |
| 2차 케이스 추가 직후 | 10/12 (83%) |
| 「할 수 없는 일을 쓰지 않는다」 규칙 추가 | 12/12 (100%, 평균 4.92점, 2회 연속) |
| 3차 확장 (21건) + 평가 스위트 자체 결함 수정 | **21/21 (100%, 평균 5.00점)** |

- **평가가 회귀를 잡고 수정을 확인하는 루프가 실제로 1회전** — 스위트의 존재 이유 입증
- 심판 판별력 반증 확인: 부실 스펙 **1/5**, 무관 스펙 **1/5** (만점만 나오는 무의미한 심판이 아님)
- **3차 확장에서 나온 4건은 전부 Builder가 아니라 평가·하네스 쪽 결함이었다** (6.9 참조).
  통과율이 높다고 스위트가 건강한 것이 아니다

### 7.3 단일 vs 팀 (동일 질문, 정답 5,268)
| | 단일 에이전트 | 팀 |
|---|---|---|
| 도구 호출 | 1회 | 2회 (서로 다른 방법) |
| 보고 내용 | 최종 합계만 | 중간값(6,633 / 1,365)까지 노출 |
| 검증 근거 | 없음 | 두 방법 교차 확인 |

- 둘 다 정답이나, 팀은 **답이 왜 맞는지 확인 가능한 형태**로 산출

#### 팀 비용 실측 (같은 질문, subagents 유무만 다른 두 스펙, 2회 측정)
서브에이전트 호출은 `task` 도구 **안쪽**에서 일어나 부모 그래프의 messages만 보면
빠진다. LangChain 콜백은 중첩 실행에도 전파되므로 그걸로 셌다.

| | 단일 | 팀(2인) | 배수 |
|---|---|---|---|
| LLM 호출 | 2회 | 6회 | **3.0배** (2회 측정 동일) |
| 입력 토큰 | 3,858 | 12,266 / 13,447 | 3.2~3.5배 |
| 출력 토큰 | 223 / 201 | 2,384 / 3,349 | **10.7~16.7배** |
| 지연 | 6.2 / 5.3초 | 26.4 / 32.7초 | 4.3~6.2배 |
| 실비 | $0.015 | $0.073 / $0.091 | **약 5~6배** |

- **팀 비용은 "호출이 3배"가 아니라 "쓰는 말이 10배 이상"에서 나온다** —
  위임마다 리더가 지시를 쓰고 팀원이 보고를 쓴다. 출력 단가가 입력의 5배라 이게 지배한다
- **팀은 변동도 크다**: 출력 토큰이 두 실행에서 40% 흔들렸다(단일은 거의 고정)
- 이 질문에서 팀은 **5~6배를 더 내고 같은 답**을 냈다 → "팀은 근거가 있을 때만"의 수치 근거
- 표본은 질문 1개 × 2회다. 절대값이 아니라 **자릿수**로 읽어야 한다

### 7.4 비용 (하루 작업 기준 실측)
| 모델 | 호출 | 입력 | 출력 | 비용 |
|---|---|---|---|---|
| `claude-sonnet-4-6` (Builder·에이전트) | 184건 | 568,192 | 177,256 | **$4.36** |
| `claude-haiku-4-5` (심판) | 38건 | 51,821 | 5,741 | $0.08 |
| **합계** | **222건** | | | **$4.44** |

- **출력이 비용의 61%** — 토큰 수는 입력이 3.2배지만 단가가 5배다. **캐싱은 입력만 줄인다**
- **심판을 Haiku로 분리한 결정이 확실히 효과를 냈다** — 38회 호출에 $0.08(전체의 2%).
  자기 채점 편향 해소와 비용 절감을 동시에 달성했다
- 최대 반복 지출은 평가 실행(1회 = Builder 21회 + 심판 호출) → 프롬프트·레지스트리 변경 시에만 실행
- Builder 시스템 프롬프트(약 2,850토큰)에 프롬프트 캐시 적용 — 2회차부터 캐시 읽기 실측 확인.
  평가 1회 기준 $0.103 → 약 $0.020

---

## 8. 한계 및 향후 과제

### 8.1 구조적 한계 (라이브러리 제약)
- **제거 불가 도구**: `read_file`(FilesystemMiddleware 필수), `task`(SubAgentMiddleware 필수)는 스펙이 요청하지 않아도 항상 노출
  - 단, `task`가 띄우는 general-purpose는 리더의 제한된 도구를 상속하므로 화이트리스트 구멍은 아님(실측 확인)
- **MCP 호출당 프로세스 재기동**: langchain-mcp-adapters 0.3.2는 연결을 유지하지 않음 → stdio 서버는 호출마다 기동 비용
- **`workspace/`는 경로 제한이지 샌드박스가 아님**: 프로세스 격리가 없으므로 내부 파일은 전부 읽힘
- **MCP 검증 대상은 커넥터이지 임의의 서버가 아님**: 세 transport를 실서버로 왕복시켰으나 상대는 자체 echo 서버 하나. 타 구현체의 인증 방식·세션 정책·스키마 방언과의 상호운용성은 미검증

### 8.2 미완 항목
| 항목 | 상태 | 비고 |
|---|---|---|
| 평가 케이스 확장 | 진행 필요 | 21건이 다시 전부 통과 → 현재는 이미 고친 결함의 재발만 감지 |
| **평가 1회 실행의 대표성** | **미해결** | Builder가 비결정적인데 기본 `repeats=1`. 배포 리포트의 "21/21"은 단일 표본 |
| **모델 ID 미검증** | **미해결** | 도구 키·MCP 서버명과 달리 자유 문자열. v0.3에서 적을 자리가 팀원 수만큼 늘어 노출면이 커졌다 |
| 타 구현체 MCP 상호운용성 | 미검증 | 실연결 상대가 자체 echo 서버 하나뿐 |

**해소된 항목** (2026-08-10)
| 항목 | 결과 |
|---|---|
| `--spec` 경로 가드레일 | `runtime/guardrail.py` + `load_spec_file()`로 공용화 — CLI·UI 모두 보장 |
| 팀 실행 비용 미측정 | 실측 완료 (7.3) — 팀은 약 5~6배 |
| 모델 최신화 | 비교 완료 — **`claude-sonnet-4-6` 유지.** 갈렸던 결과가 프롬프트 모순 탓으로 밝혀져 품질 차이 미입증. 6케이스 × 1회로 기본값을 바꾸는 것은 잡음에 반응하는 것 |

### 8.3 다음 확장 축 (평가 케이스)
소진한 축: 모순 요구, 화이트리스트 밖 능력, MCP 얽힘, 도구 3~4개,
사용자 도구 지정, 팀원 간 의존, 출력 형식, 다국어, 프롬프트 지침 충돌.

다음 후보:
- 요구가 아주 긴 경우(3문단 이상)
- 도메인 용어가 많은 요구
- 이미 만든 에이전트를 수정해달라는 요구
- **같은 요구를 반복했을 때의 일관성** — 이번에 비결정성이 실제로 확인됐다

---

## 9. 결론

- 자연어 → 에이전트 생성·실행·평가의 **전 경로를 실호출로 검증** 완료
- 프로젝트의 핵심 주장인 **"제약을 코드로 강제한다"** 를 실측으로 입증
  - 도구 화이트리스트가 서브에이전트 위임 경로에서도 유지됨
  - 가드레일 문장이 리더·팀원 전원에 주입됨
  - 파일 접근이 지정 디렉터리를 벗어나지 못함
- **가장 큰 수확은 검증 과정 자체**
  - 단위 테스트 통과와 실제 동작은 별개임을 9건의 결함으로 확인
  - 특히 **통과하던 보안 테스트가 아무것도 검증하지 못하던 사례**는, 테스트의 존재가 아니라 **테스트가 무엇을 확인하는지**가 중요함을 보여줌
  - 평가 스위트 역시 "전부 통과"는 품질 신호가 아니라 **감지력 부재 신호**일 수 있음

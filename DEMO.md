# 데모 촬영 대본

> 목표 분량 **5~7분**. 이 문서의 모든 명령은 실행해 확인한 것이다.
> 실측 근거는 [BUILD_SPEC.md](BUILD_SPEC.md), 설계 서술은 [REPORT.md](REPORT.md).

---

## ⚠️ 촬영 전 필수 확인

- [ ] **`.env`를 화면에 열지 않는다.** `cat .env`, 편집기로 열기, 터미널 스크롤 모두 금지.
      UI 사이드바는 키의 **존재 여부만** 표시하므로 안전하다
- [ ] `specs/` 를 비운다 — 생성 장면에서 새로 만들어지는 것이 보여야 한다
- [ ] `workspace/` 에 개인 문서를 두지 않는다 (화면에 목록이 뜬다)
- [ ] 터미널 글꼴 크기를 키운다. 코드가 안 읽히면 장면의 의미가 없다
- [ ] 팀 실행은 **30초~1분** 걸린다. 편집으로 줄이거나 대기 중 설명을 얹는다

```bash
./.venv/Scripts/python.exe -m pytest tests/ -q -m "not integration"   # 사전 점검
```

---

## 장면 1 — 문제 (30초)

말할 것:

> "LLM에게 에이전트를 만들라고 하면, 프롬프트에 제약을 적어 **부탁**하게 됩니다.
> 그런데 LLM은 프롬프트를 어깁니다. 이 프로젝트는 부탁 대신 **코드로 강제**합니다."

화면: [README.md](README.md)의 「동작 방식」 다이어그램.

---

## 장면 2 — 자연어로 만들기 (1분 30초)

```bash
python cli.py "웹에서 자료를 찾아 계산하고 결과를 파일로 저장해주는 에이전트 만들어줘"
```

가리킬 것:

- `spec tools`에 **`web_search`·`calculate`·`file_write` 셋 다** 잡힌다
  → "동사가 셋이면 도구도 셋"이라는 규칙이 작동한다
- `subagents : (none)` → 한 사람이 순서대로 하면 되는 일이라 팀을 만들지 않았다
- `builtin fs : read_file (+ task)` → deepagents가 강제로 주입하는, **제거할 수 없는** 도구.
  숨기지 않고 그대로 보여준다

> 대사: "요구하지 않은 셸 실행 도구는 없습니다. 스펙에 없으면 붙지 않습니다."

---

## 장면 3 — 강제가 진짜인지 (1분)

```bash
python cli.py "서버에서 셸 명령을 실행해주는 에이전트 만들어줘" --no-chat
```

가리킬 것: 셸 도구가 **붙지 않고**, `description`에 *할 수 없다*고 적힌다.

> 대사: "화이트리스트에 없는 능력은 프롬프트로 부탁해도 붙지 않습니다.
> 다른 도구로 우회하지도 않습니다."

이어서 계산 도구가 코드 실행이 아님을 보인다:

```bash
python cli.py --spec templates/data_analysis_team.json
```
```
you> 1부터 200까지 3의 배수이면서 5의 배수가 아닌 수의 합 구해줘
```

- analyst가 계산 → reviewer가 **다른 방법으로** 검산 → 리더가 일치 확인 (정답 5,268)
- 두 팀원의 도구는 `calculate` 하나뿐 — **임의 코드 실행 권한이 없다**

> 대사: "예전에는 이런 계산에 파이썬 실행 도구를 붙였습니다. 그건 사실상 셸입니다.
> 지금은 수식만 평가하는 도구가 맡고, 평가 21개 케이스에서 코드 실행 도구 선택은 **0건**입니다."

---

## 장면 4 — 자연어로 고치기 (1분 30초) ★ 하이라이트

장면 3의 대화를 **그대로 이어서**:

```
you> /revise 계산 결과를 파일로도 저장해줘
```

가리킬 것:

```
[변경 내역]
  ~ 팀원 analyst
      system_prompt: (292자) → (414자)
      + 도구 file_write
```

- **무엇이 바뀌었는지 전부 보인다.** 요청하지 않은 변경이 섞이면 여기서 드러난다
- 도구만 붙는 게 아니라 **그 도구를 언제 쓰는지 프롬프트에도 반영**된다
- 원본 템플릿은 그대로고 수정본만 `specs/`에 저장된다

이어서 새 능력 확인:

```
you> 방금 계산 결과를 result.md 로 저장해줘
```

> 대사: "만들고 끝이 아니라, 고치고 다시 쓰는 루프가 닫혀 있습니다."

---

## 장면 5 — 회귀를 어떻게 아는가 (1분)

```bash
streamlit run ui/app.py
```

- **사이드바**: 키는 **존재 여부만** 표시된다 (값이 화면에 안 나온다는 점을 말한다)
- **평가 탭**: 케이스 21건 실행 → 통과율과 심판 점수

말할 것:

> "평가 대상은 **Builder가 요구를 옳은 명세로 옮기는가**입니다.
> 생성된 에이전트의 답변 품질이 아닙니다 — 그건 도구·모델·프롬프트가 섞인 결과라
> 회귀 신호로 못 씁니다."

> "이 스위트가 실제로 결함을 잡았습니다. 도구 과소 선택, 없는 능력을 있다고 쓰는 명세,
> **비밀값을 그대로 넘기던 도구**까지."

⏱ 평가 1회는 Builder 21회 호출이라 **미리 돌려 결과 화면을 준비**하거나 편집으로 줄인다.

---

## 장면 6 — 정직한 한계 (45초)

화면: [README.md](README.md) 「알려진 한계」

말할 것:

> "`workspace/`는 경로 제한이지 샌드박스가 아닙니다.
> `python_repl`은 기본으로 붙지 않게 만들었지만 없애지는 않았고, 붙이면 여전히 위험합니다.
> 평가는 케이스당 1회만 돌아서 '21/21'은 단일 표본입니다."

> "무엇을 못 하는지 아는 것이 이 프로젝트에서 가장 오래 걸린 부분입니다.
> 보안 테스트 9건이 **잘못된 이유로 통과**하고 있던 적도 있습니다."

---

## 촬영 순서 요령

1. **장면 5의 평가 결과를 먼저 확보**한다 (오래 걸리므로)
2. 장면 3~4는 **한 세션에서 끊지 않고** 찍는다 — `/revise`가 대화를 이어받는 것이 요점이다
3. 도구가 거부되는 순간은 다시 찍지 말고 그대로 두는 편이 낫다

## 하지 말 것

- 없는 기능을 있는 것처럼 말하지 않는다 (이 프로젝트가 Builder에게 요구하는 것과 같은 기준)
- "완전히 안전합니다" 류의 표현 — 문서가 스스로 정정하게 된다
- 키·토큰이 보이는 화면

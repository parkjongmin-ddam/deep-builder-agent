"""평가 레이어 (Phase 4) — Builder 출력의 회귀를 잡는다.

주의: 패키지 이름이 내장 함수 `eval`과 같다. `import eval` 형태로 쓰면 그
네임스페이스에서 내장 `eval()`이 가려진다. **항상 `from eval.x import y` 형태로
임포트한다** (BUILD_SPEC.md가 지정한 디렉터리 이름이라 그대로 따른다).
"""
